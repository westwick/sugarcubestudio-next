# MailerLite API key for mailing list signups (mailing-list page).
# Get your key from https://dashboard.mailerlite.com/ (or connect.mailerlite.com).
# This is used in the browser; prefix with NEXT_PUBLIC_ so Next.js exposes it.
NEXT_PUBLIC_MAILERLITE_API_KEY=
